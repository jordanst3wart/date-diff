#!/usr/bin/env python3
"""
Tells the difference between two dates
"""
import argparse

import logging

FORMAT = "%(asctime)s - %(levelname)s: %(message)s"
logging.basicConfig(level=logging.INFO, format=FORMAT)
logger = logging.getLogger(__name__)


# This is a sample Python script.

# Press Shift+F10 to execute it or replace it with your code.
# Press Double Shift to search everywhere for classes, files, tool windows, actions, and settings.


def diff_dates(start_date, end_date):
    try:
        # start = date(start_date) # can be invalid
        # end = date(end_date) # can be invalid
        # diff = end.diff(start)
        # if negative error
        print("not implemented")
    except:
        logger.error("Invalid start, or end date")
        raise


def main():
    parser = argparse.ArgumentParser(
        description="""
        CLI tool to tell the difference between dates.
        """
    )

    parser.add_argument("-s", "--start_date", type=str, help="Start date")
    parser.add_argument("-e", "--end_date", type=str, help="End date")

    kwargs = vars(parser.parse_args())
    diff_dates(**kwargs)


if __name__ == "__main__":
    main()
